import { Box, Typography } from '@mui/material';

const Loans = () => {
    return (
        <Box>
            <Typography variant="h4">Loans</Typography>
        </Box>
    );
};

export default Loans;
