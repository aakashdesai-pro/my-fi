import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#ffcd1c',
            light: '#ffde6b',
            dark: '#c79c00',
        },
        secondary: {
            main: '#0d47a1',
            light: '#5472d3',
            dark: '#002171',
        },
    },
});

export default theme;
